<UserControl x:Class="Brew3.Views.OrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Мои заказы" 
                   FontSize="28" 
                   FontWeight="SemiBold" 
                   Foreground="#616F53" 
                   Margin="0,0,0,20"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="OrdersList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#E0DCCD" 
                                BorderThickness="1" 
                                CornerRadius="6" 
                                Padding="15" 
                                Margin="0,0,0,15"
                                Background="White">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock>
                                            <Run Text="Заказ №" FontWeight="Bold"/>
                                            <Run Text="{Binding OrderNumber}"/>
                                            <Run Text=" от " FontWeight="Bold"/>
                                            <Run Text="{Binding CreatedAt, StringFormat=dd.MM.yyyy HH:mm}"/>
                                        </TextBlock>
                                        <TextBlock>
                                            <Run Text="Статус: " FontWeight="SemiBold"/>
                                            <Run Text="{Binding Status}" Foreground="#8E9F6D"/>
                                        </TextBlock>
                                    </StackPanel>
                                    <Button Grid.Column="1"
                                            Content="Закрыть"
                                            Tag="{Binding}"
                                            Click="CloseOrder_Click"
                                            Background="#D32F2F"
                                            Foreground="White"
                                            BorderThickness="0"
                                            Padding="15,5"
                                            FontSize="14"
                                            FontWeight="SemiBold"
                                            Cursor="Hand"
                                            Margin="10,0,0,0"/>
                                </Grid>
                                <TextBlock FontWeight="SemiBold" Margin="0,10,0,5">Состав:</TextBlock>
                                <ItemsControl ItemsSource="{Binding OrderItems}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding Product.Name}" Width="200"/>
                                                <TextBlock Text="×" Margin="5,0"/>
                                                <TextBlock Text="{Binding Quantity}" Margin="0,0,10,0"/>
                                                <TextBlock Text="{Binding Product.Price, StringFormat={}{0:C}}" />
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <TextBlock FontWeight="Bold" HorizontalAlignment="Right" Foreground="#616F53">
                                    Итого: <Run Text="{Binding TotalPrice, StringFormat={}{0:C}}" />
                                </TextBlock>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>