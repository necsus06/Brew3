<UserControl x:Class="Brew3.Controlers.ProductsUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Brew3.Controlers"
             mc:Ignorable="d" 
             Width="950"
             Height="270"
             d:DesignHeight="270" d:DesignWidth="950">
    <Border BorderBrush="#E0DCCD" 
            BorderThickness="1"
            CornerRadius="6"
            Background="White">
        <Grid Margin="15,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <Image x:Name="BoxImage"
                       Height="250" 
                       Width="300"
                       Stretch="UniformToFill"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center">
                    <Image.Clip>
                        <RectangleGeometry Rect="0,0,300,250"/>
                    </Image.Clip>
                </Image>
            </StackPanel>
            <StackPanel Margin="10,0,0,0"
                     Grid.Column="1"
                     VerticalAlignment="Top">
                <TextBlock FontSize="20" FontWeight="Bold">
                 <Run Text="{Binding Category}" />
                 <Run Text=" | " />
                 <Run Text="{Binding Name}" />
                </TextBlock>
                <TextBlock FontSize="20">
                 <Run Text="Описание товара: "/>
                 <Run Text="{Binding Description}"/>
                </TextBlock>
                <TextBlock FontSize="20">
                 <Run Text="Цена: " />
                 <Run Text="{Binding Price}"
                      Name="BoxPrice" />
                 <Run Name="BoxNewPrice" />
                </TextBlock>
                <Button Content="Добавить" Background ="#616F53" Foreground ="White" FontSize="20" FontWeight="SemiBold" Width="120" HorizontalAlignment="Left" Margin="0,10,0,0" Click="Button_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
